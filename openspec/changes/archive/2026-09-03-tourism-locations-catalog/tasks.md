## 1. Content and Foundation

- [x] 1.1 Add representative local image assets for the curated Catamarca places and verify every catalog entry has a matching image file.
- [x] 1.2 Replace the starter-page content with a Spanish introduction and a local collection of tourist places containing name, locality, category, and description; verify all required fields render for every card.
- [x] 1.3 Update the root metadata and document language for Turismo Catamarca; verify the page title and language attribute in the rendered document.

## 2. Catalog Presentation

- [x] 2.1 Implement responsive tourist-place cards that show the representative image and required place information; verify the catalog uses one column on narrow viewports and multiple columns on desktop.
- [x] 2.2 Apply page styles that provide readable contrast, spacing, and a clear visual hierarchy; verify no horizontal page scrolling occurs at a 320px viewport.

## 3. Verification

- [x] 3.1 Run `npm run lint` and `npm run build` and verify both commands succeed.
